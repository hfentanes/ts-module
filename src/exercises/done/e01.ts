// START: Follow the instructions below.

// Add type annotations to each of these variables.
const country: string = "Italy";
const year: undefined = undefined;
const averageTemperature: number = 23.6;
const visited: boolean = true;
const currency: null = null;

// Change the type annotations on these variables so they are correct.
const population: number = 47_450_795;
const isSummer: boolean = false;
const languages: string = "Spanish, English";

export {
  country,
  year,
  averageTemperature,
  visited,
  currency,
  population,
  isSummer,
  languages,
};
